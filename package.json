{
  "name": "fetch-timeout",
  "version": "0.0.2",
  "description": "HTTP/S fetch wrapper that adds the possibility to set a timeout after which a custom error is returned.",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src --fix",
    "pretest": "npm run lint",
    "test": "mocha --reporter spec",
    "cover": "istanbul cover mocha --report lcovonly -- -R spec"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jkomyno/fetch-timeout.git"
  },
  "keywords": [
    "fetch",
    "timeout",
    "promise"
  ],
  "author": "Alberto Schiabel <alberto.schiabel@gmail.com> (https://github.com/jkomyno)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jkomyno/fetch-timeout/issues"
  },
  "homepage": "https://github.com/jkomyno/fetch-timeout#readme",
  "peerDependencies": {
    "node-fetch": "^1.6.3"
  },
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "chai": "^3.5.0",
    "coveralls": "^2.12.0",
    "eslint": "^4.4.1",
    "eslint-config-airbnb-base": "^11.3.1",
    "eslint-plugin-import": "^2.7.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "node-fetch": "^1.6.3"
  }
}
